<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds: allow HTTP to any host (local FastAPI on LAN IP). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
